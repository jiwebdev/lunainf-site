## Summary

The architecture page now offers two views of the same reviewed public artifact: an organic family overview for orientation, followed by the full technical explorer for all 46 subsystems and 57 relationships across eight families. Model inference occupies a bounded side region.

- Derives overview copy, counts, representatives and 14 family corridors (35 cross-family relationships) from the unchanged canonical public artifact. Preserves directional counts and canonical Inference Fabric membership in Cognition.
- Adds hover, focus, click/tap selection, readable mobile family controls, and a handoff to the full explorer's existing family filter.
- Routes the 14 derived corridors through validated presentation-only boundary ports. Resting cables are faint; active cables use a translucent band and five inner strands, with separate active/connected/focus treatments.
- Subdues the full graph at rest while retaining all direct incoming/outgoing connection highlighting, search, inspector, pan/zoom, directory and no-JavaScript fallback.
- Converges homepage, LUNA, ChronoForge, Progressive Recall and White Paper framing to the reviewed maturity model. Realtime Conversation remains Experimental and deliberately configured within the existing conversation and tool boundaries.
- Provides strict artifact/projection validation and static-only build checks. No runtime architecture data or new visualization dependencies.

## Validation

- `npm ci` and `npm run validate` pass.
- Astro check: zero errors, warnings or hints; static build: 15 pages; static boundary: 23 files; internal links: 298.
- Projection tests cover representative membership/uniqueness, canonical copy/counts, directional bundles, deterministic ordering, annotation and no-invented-edge fixtures.
- Chrome smoke at 1440px and touch-enabled 390px passes: eight regions, selection, ribbon emphasis, details, family handoff, explorer direct relationships, search, no overflow and mobile inspector placement.
- Keyboard focus, Space/Escape, hover and no-JavaScript directories pass. Desktop/mobile screenshots reviewed locally.
- `git diff --check` passes.
- Final routing pass: desktop rest/Cognition/Memory/Inference and matching 390px states captured and reviewed. All active sets match canonical bundles; Inference reveals only Cognition?Inference. The desktop curve-sampling check found no outer-band intersections with heading, description, pill or additional-count boxes.
- Route rejection fixtures cover unknown families, nonexistent bundle pairs, missing routes/ports, empty/non-finite geometry and endpoint mismatch. Canonical topology, directions and widths remain unchanged.

## Review notes

See [the overview review note](docs/architecture-overview-review.md) for every representative, family count, directional corridor count, thickness calculation and interaction detail.

Final human review should assess active cable prominence and strand subtlety at phone scale. The accepted composition and numbered mobile treatment are preserved; no remaining text obstruction was observed in reviewed states. Inference Fabric remains in its canonical Cognition family; the approved inference annotation covers it and both providers.

The existing Astro audit advisories (one low, two high) remain a separate maintenance concern. Historical White Paper and Early LUNA material remain historical. No live Brain video was added. Pages target/domain are unchanged; HTTPS enforcement remains a separate settings concern.

Do not merge automatically. No manual deployment is included.

