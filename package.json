{
  "name": "lunainf-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "verify:architecture": "tsx scripts/verify-public-architecture.ts",
    "verify:static": "node scripts/verify-static-boundary.mjs",
    "verify:links": "node scripts/verify-internal-links.mjs",
    "validate": "npm run verify:architecture && npm run check && npm run build && npm run verify:static && npm run verify:links"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.2.1",
    "astro": "^5.13.5"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "tsx": "^4.23.13",
    "typescript": "^5.9.3"
  }
}
